model Product {
  id          String   @id @default(cuid())
  name        String
  sku         String   @unique
  description String?
  brand       String?
  price       Float
  imageUrl    String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
